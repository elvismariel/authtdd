{
  "name": "authtdd",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js --ignore __tests__",
    "pretest": "NODE_ENV=test jest sequelize db:migrate",
    "test": "NODE_ENV=test jest",
    "postest": "NODE_ENV=test jest sequelize db:migrate:undo:all"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.12.1",
    "sequelize": "^5.21.2"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^1.19.4",
    "sequelize-cli": "^5.5.1",
    "sqlite3": "^4.1.0"
  }
}
